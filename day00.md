# linux基础

## 文件权限管理命令
  +    查看文件权限
       +  ls -l
  +    文件类型
       +  'd' 文件夹
       +  'c' 字符设备文件
       +  'b' 块设备文件
       +  '-' 普通文件
       +  'l' 链接文件
  +   文件权限
       +  'r'   读
       +  'w'   写
       +  'x'   执行

  +   权限分组
      +    用户权限（user）
      +    组权限(group)
      +    其他权限(other)
  +   最高权限
      +    rwxrwxrwx
  +   最低权限
          --- --- ---
  +   属主：owner
  +   属组：group

| rw-  |rw-  |r--   |  tarena  | tarena|
| -----|-----|------|----------|-------|
| 用户 |组   |其他  |  属主    | 属组  |
     
## chmod命令

**作用：**
           
 修改文件权限
                 
**格式：**
           
chmod  权限   文件名/文件夹名
                 
**权限：**

+   u 用户（属主）
+   g同组用户
+   o其他用户
+   a所有用户
+   +增加权限
+   -减少权限
              
**示例：**

+   chmod  u-r a.txt
+   chmod  o-rwx a.txt
+   chmod g+rw a.txt

## 输出重定向

**两种：**

+  1.标准输出
+  2.标准错误输出

**标准输出重定向**

+  '>  '将一个命令的标准输出重定向到指定文件
+  '>>' 将一个命令的标准输出重定向“追加“到制定文件,标准错误输出重定向

**标准错误输出重定向**

+  2>  将一个命令的标准“错误”输出重定向到指定文件
+ 2>> 将一个命令的标准“错误”输出重定向“追加“到制定文件  ，重定向所有输出


**示例：**     
```python
find /etc/  -name  "passwd"
stdout.txt  #界面输出错误信息，正确信息输出到stdout.txt文件
stderr.txt #所有信息输出到文件
```

# Python语言开发基础
             
## python　简介

+ 创始人：Guido van Rossum(荷兰人)
  +  时间：1989，圣诞节
  +  命名：Python，喜剧团Monty Python，粉丝

+ python应用领域
  + 系统运维
  + 网络编程
  + 科学计算
  + 人工智能、机器人
  + web开发
  + 大数据及数据库编程
  + 云计算
  + 教育
  + 游戏，图像
  + 其他
+ 优缺点
  + 优点：
      + 面向对象（java,c++,python）
      + 免费
      + 可移植
      + 可以混合变成
      + 简单易学易用
      + 应用范围广泛
      + 开发效率高
      + 开源
  + 缺点：
      + 与C/C++相比执行速度不够快
      + 不能封闭源代码
+ python版本
      + python2.7(2020结束维护) 
      + python3.5（教学版本）
      + python3.8 （最新版本）    

+ python安装
      +windows/ mac os x/ Unix


+ python运行
      + python的解释执行器
      + Cpython
      + Jython
      + IronPython
      + PyPy
+ 第一个python程序
      + hello.py
+ python的运行
      + 方法1
        + $python3  hello.py
      + 方法2
        + 在hello.py的第一行写入：
        + /usr/bin/python3
        + $chmod u+x hello.py
        + $./hello.py     

+ 交互模式
      + $python3



## python程序的组成

  +   程序由模块组成
  +   模块又语句，函数，类，数据等组成
  +   语句包含表达式
  +   表达式建立并处理数据对象

**python的注释**

 #井号开头，直到结尾

**python的核心数据类型**

+ 数字（整数、浮点数、复数、布尔型）
+ 字符串
+ 列表
+ 整型数 int
  + 不带小数部分的数字
  + 十进制表示：-5，0，10
  + 二进制表示(0b开始，后跟0~1)：0b111,0b101,0b11111111
  + 八进制表示（0o开头，后跟0~7）：0o177,
  + 十六进制（0x开头，后跟0~9，A-F,a-f）:0xFF

**进制转换：**

+ 十进制（10进1）
+ 二进制（2进1）
+ 八进制（8进1）
+ 十六进制（16进1）

**浮点型数float**

+ 带有小数部分的数字
   + 表示方式：3.14， 3. ， 0.14， .14
+ 科学计数法： 
   + 格式：小数 e/E(正负号) 指数
      + 如： 6.18E-1,2.9979e8 

**复数 complex**

分为两部分

+  实部（real）
+  虚部（image）,以j或J结尾

字面值：
+ 1j,
+ (2j)，
+ 1+1J，
+ 3-4J，
+（-100 + 100J）

**布尔类型数  bool**

用来表示真和假两个状态的类型

+ True ，1， 真 
+ False，0， 假
+ 无类型：空值,None,对象
    + None， 表示不存在的特殊对象
    + 作用：
      + 用来占位，
      + 用来变量接触绑定           
 

## 表达式和运算符
     
### 表达式  expression
由一个数字或数字和运算符组成

**作用：**

让计算机做一些事情并返回结果

**例子：**

1+2

### 运算符：

+ '+' 
+ '-'
+ '*'
+ '/' 
+ '//地板除'
+ '%求余'
+ '**幂运算'   

**除法：**

得到浮点数，不会丢弃小数部分

+  1/3   = 0.3333333333333333
+  4/2   = 2.0

**地板除：**

除的结果去掉小数部分向下取整

+ 7/3 = 2.333333333
+ 7//3 =2
+ 3.25//1.5 = 2.0

**运算符优先级**
+ '**'
+ '*  /   //   %'
+ '+   -'

**括号分组子表达式**

+ 用括号将表达式分组
+ 混合类型自动升级 int --float --complex 
+ 1+2.14 返回浮点数 3.14
  
       
## 基本输出函数 print
**格式:** 

print(要打印的数据）

数据可以用‘，’分隔开

##变量 variable
用来绑定数据对象的标识符

**变量名的命名方法：**

+  必须为字母或下划线开头，后跟字母或下划线
+  不能使用python的关键字做变量名

## python常见关键字：

+ True,
+ False,
+ None, 
+ is, 
+ del, 
+ if, 
+ elif等    

## 赋值语句 statement

**语法：**

```python
变量名 = 表达式 
      或 
            变量名1 = 变量名2 = 表达式
      或
            变量名1，变量名2，... = 序列
```

**作用：**

用于创建变量并将变量绑定（或关联）在一个对象上

**说明：**

+ 当变量不存在时，创建该变量同时绑定在这个对象上
+ 当变量存在是，改变此变量的绑定对象
+ 一个变量只能绑定一个对象
+ 两个变量可以同时绑定同一个对象

**示例：**

```python
a = 10 #创建一个变量，同10绑定一起
b = 20
c = a + b
pi3=pi2=pi1 = 3.1415926
x,y = 100,200
```
**python的关联、绑定、引用的含义**

+ python中，变量没有类型
+ 关联、绑定、引用都是指变量和对象的关联关系
+ 在交互模式下查看变量间绑定关系
   + help('__main__'）

## python自动内存管理
```python
a= 10000
b= 20000
c= a + b
```
## 序列赋值语法
```python
变量名1，变量名2，... = 对象1，对象2，...
变量名1，变量名2，... = (对象1，对象2，...)
变量名1，变量名2，... = [对象1，对象2，...]
```      
**变量的交互绑定练习(交换算法)**
+ a 绑定10000
+ b 绑定20000

不创建新对象的情况下，如何让a,b交互绑定对象

+ a=10000
+ b=20000
+ a,b = b,a

## is / is not 运算符

**作用：**

判断两个对象是否绑定同一个对象，如果是同一个对象返回True,否则返回False
          
is not 与is 作用相反
     
**语法：**
```python
x is y
x is not y
```
**示例：**
```python
a,b=10000,10000
print("a is b is",a is b) #Fals
c=d=10000 
print("c is d is", c is d) #True   
```
 
## 小整数对象池
CPyhton中整数 -5 至256 永远存在小整数池，不会释放并可重复使用
         
**示例：**
```python
a = 5
b = 5
a is b  # True ？？？小整数对象池

a = 257
b = 257
a is b  # False ？？？

a  = 257; b = 257
a is b  #True ？？？Python内存优化

x = 10000
y = 10000
print(id(x))
print(id(y))
print(x is y) #True ???代码段，python使用同一个对象，节约内存
```

## id()函数

**格式：**

id（对象）

**作用：**

返回一个对象在内存中的地址 

1. 如果 a is b = True，那么 id值是相同的

2.小整数池内的数值的id是不变的。
 
## del()函数

**作用：**

用于删除变量，同时删除与对象的关联关系。如果可能则释放对象

**语法：**
```python
del 变量名
```

## python的内存管理和引用计数
- 1.每个对象都会记录有几个变量引用自身，当引用数量为0时，则对象被销毁，
- 2.此种自动化内存管理的方式叫引用计数。
- 3.引用计数为0时，对象会释放

```python
a = 10
del a #10对象释放掉
a = 10 #10对象会记录有几个变量绑定自己，引用计数
b = a
del a #b还在，对象还在      
```

## 复合数值复制运算符

```python
y += x     等同于 y = y + x
y  -= x    等同于 y = y - x
y  *= x    等同于 y = y * x
y  /= x    等同于 y = y / x
y //= x    等同于 y = y // x
y %= x     等同于 y = y % x
y **= x    等同于 y = y ** x
```                
 