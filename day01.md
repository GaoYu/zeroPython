# day01笔记
## 字符串str
### 作用
用来记录文本信息
###表示方法 
在非注释中，凡是引号括起来的部分都是字符串
''单引号
""双引号
'''三单引号
"""三双引号
单引号和双引号的区别：
单引号内双引号不算结束符
双引号内的单引号不算结束符
##三引号字符串：
###作用：
三引号字符串的换行会自动转换为换行符'\n'
三引号内可以包含单引号和双引号
###如何表示：
''' Welcome to beijing
    I like python!
	I am Studing!''

##转义序列
\n  \t \\  \'  \"  \xXX  \uXX

##编码
ASCII 美国信息交换标准码（128个）
UNICODE16(65536个编码)  UNICODE32（2**32个）

##raw字符串
r''
r""
r''''''
与普通字符串区别：r字符串的\不作为转义字符
##字符串运算
+，拼接
*，重复多少倍
+=
*=
<
<=
>
>=
==
!=
in  /  not in
索引和切片[]

##常用函数
len(x)
max(x)
min(x)

ord(c) 给字符返回编码
chr(i) 给编码返回字符

hex(i) 十六进制
bin(i) 二进制
oct(i) 八进制转换

str(obj = '') 转字符串

##常用字符串方法

见str.html
字符串方法的调用语法：
对象.方法名(方法传参)
注：方法的调用表达
示例：
   'abc'.isalpha()
   123.isalpha()
